.DS_Store

# Xcode user-specific state
*.xcuserstate
xcuserdata/

# Xcode and SwiftPM build products
build/
DerivedData/
.build/

# SwiftPM resolution artifacts when this repo adds packages later
.swiftpm/
Package.resolved

# Aider session scratch files
.aider.chat.history.md
.aider.input.history
.aider.tags.cache.v4/
deps/built/

# Distribution artifacts — only DMGs are tracked, not app bundles
dist/*.app
